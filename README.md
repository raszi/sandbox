# Hello there!

## Syntax highlighting test

```javascript
    var something = 'Hello'
    console.log(something);

    var data = [ 'foo', 'bar' ];
    data.forEach(function (item) {
        console.log(item);
    });
```
